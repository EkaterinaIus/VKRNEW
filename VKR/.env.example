SECRET_KEY=django-insecure-replace-me-with-50-random-chars
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=reading_db
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_HOST=localhost
DB_PORT=5432

# ── Email ──────────────────────────────────────────────────────────────
# В РАЗРАБОТКЕ: письма выводятся в терминал (ничего настраивать не нужно)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# В ПРОДАКШНЕ: раскомментируйте строки ниже и заполните реальными данными
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your@gmail.com
# EMAIL_HOST_PASSWORD=xxxx xxxx xxxx xxxx   # Gmail App Password (16 символов)
# DEFAULT_FROM_EMAIL=your@gmail.com
